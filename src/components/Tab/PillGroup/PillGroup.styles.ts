import fonts from '../../../styles/fonts';
import resetCSS from '../../../styles/reset';

const initialStyles = `
    ${resetCSS}
    ${fonts.text}
`;

export const pillGroupStyles = {
  initialStyles,
};
