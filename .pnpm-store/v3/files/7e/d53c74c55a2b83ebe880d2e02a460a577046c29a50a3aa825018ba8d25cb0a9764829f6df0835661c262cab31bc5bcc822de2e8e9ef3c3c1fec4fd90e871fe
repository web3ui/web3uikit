/**
 * Resolve `ref` relative to `file` (not a directory).
 *
 * The `ref` must begin with `./` or `../` or equal `.` or `..`
 */
export declare function relative(file: string, ref: string): string | null;
