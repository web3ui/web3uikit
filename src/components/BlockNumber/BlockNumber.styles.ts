import styled from 'styled-components';
import fonts from '../../styles/fonts';



const BNdiv = styled.span`
    ${fonts.semiBold}
    white-space: nowrap;
    line-height: 25px;
`;

export const BNStyles = {
    BNdiv,
};

export default BNStyles;