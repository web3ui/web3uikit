import type { GitLabReleaseSchema } from './schema';
export default function runExecutor({ tag, ref, assets, description, milestones, name, releasedAt, }: GitLabReleaseSchema): Promise<{
    success: boolean;
} | {
    success: boolean;
}>;
