class {
    onCreate(input) {
        this.onClick = input.onClick;
    }

    handleClick() {
        this.input.onClick();
    }
}

<div>
  <button type="button" on-click("handleClick")>${input.children}</button>
</div>
