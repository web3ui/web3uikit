export declare function formatTagPrefix({ versionTagPrefix, projectName, syncVersions, }: {
    versionTagPrefix: string | null | undefined;
    projectName: string;
    syncVersions: boolean;
}): string;
export declare function formatTag({ tagPrefix, version, }: {
    tagPrefix: string;
    version: string;
}): string;
