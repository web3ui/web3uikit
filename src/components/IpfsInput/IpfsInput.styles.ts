import styled from 'styled-components';
import resetCSS from '../../styles/reset';



const InputButton = styled.input`

    ${resetCSS}
    display: inline-flex;
    overflow: hidden;
`


export const IpfsInputStyles = {
    InputButton
    
};
