import { HeadersObject } from '../glossary';
/**
 * Converts a given `Headers` instance into a plain object.
 * Respects headers with multiple values.
 */
export declare function headersToObject(headers: Headers): HeadersObject;
