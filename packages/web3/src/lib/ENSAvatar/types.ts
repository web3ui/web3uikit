export interface ENSAvatarProps {
    /**
     * Address/ENS name to show the avatar (Avatar on current chain is shown)
     */
    address: string;

    /**
     * Size of the avatar
     */
    size?: number;
}
